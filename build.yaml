include:
  # Left Side with ZMK Studio Enabled
  - board: nice_nano_v2
    shield: oc0_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
    artifact-name: oc0_left_studio

  # Right Side (Standard)
  - board: nice_nano_v2
    shield: oc0_right

  # Reset Keymap (Standard)
  - board: nice_nano_v2
    shield: settings_reset   

  - board: nice_nano_v2
    shield: tester_pro_micro   
